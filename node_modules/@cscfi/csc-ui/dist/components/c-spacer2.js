import { proxyCustomElement, HTMLElement, h, Host } from '@stencil/core/internal/client';

const cSpacerCss = "html{box-sizing:border-box}body{margin:0}*,*:before,*:after{box-sizing:inherit;-webkit-tap-highlight-color:transparent}:host{font-family:var(--c-font-family)}::-ms-reveal{display:none}:host{flex-grow:1 !important}";

const CSpacer = proxyCustomElement(class CSpacer extends HTMLElement {
  constructor() {
    super();
    this.__registerHost();
    this.__attachShadow();
  }
  render() {
    return h(Host, null);
  }
  static get style() { return cSpacerCss; }
}, [1, "c-spacer"]);
function defineCustomElement() {
  if (typeof customElements === "undefined") {
    return;
  }
  const components = ["c-spacer"];
  components.forEach(tagName => { switch (tagName) {
    case "c-spacer":
      if (!customElements.get(tagName)) {
        customElements.define(tagName, CSpacer);
      }
      break;
  } });
}

export { CSpacer as C, defineCustomElement as d };

//# sourceMappingURL=c-spacer2.js.map