import { EventEmitter } from '../../stencil-public-runtime';
export declare class CTextField {
  internals: ElementInternals;
  autofocus: boolean;
  autocapitalize: string;
  autocorrect: string;
  autocomplete: string;
  disabled: boolean;
  hideDetails: boolean;
  hint: string;
  hostId: string;
  trimWhitespace: boolean;
  label: string;
  max: number;
  min: number;
  name: string;
  placeholder: string;
  readonly: boolean;
  required: boolean;
  rows: number;
  shadow: boolean;
  step: number;
  type: string;
  valid: boolean;
  validate: boolean;
  validateOnBlur: boolean;
  validation: string;
  value: string;
  changeValue: EventEmitter;
  isFocused: boolean;
  labelWidth: number;
  preSlotWidth: number;
  hiddenEl: HTMLCTextFieldElement;
  private _inputElement;
  private _originalType;
  private _inputId;
  private static _uniqueId;
  componentWillLoad(): void;
  get isActive(): boolean;
  get passwordIcon(): string;
  private _handleChange;
  private _handleBlur;
  private _renderInputElement;
  private _isFirefox;
  private _renderDateToggle;
  private _toggleDatepicker;
  private _renderPasswordToggle;
  private _togglePasswordVisibility;
  render(): any;
}
