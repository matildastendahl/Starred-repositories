import { EventEmitter } from '../../stencil-public-runtime';
export declare class COtpInput {
  internals: ElementInternals;
  hideDetails: boolean;
  hint: string;
  elementId: string;
  length: number;
  valid: boolean;
  validation: string;
  value: string;
  changeValue: EventEmitter<string>;
  completion: EventEmitter<string>;
  statusText: string;
  reset(): Promise<void>;
  private _backSpacePressed;
  private _debounce;
  private _isPasting;
  private _inputs;
  private static _uniqueId;
  onValidationMessageChange(): void;
  onValueChange(value: string): void;
  private _emitValue;
  private _getElements;
  private _onFocus;
  private _onKeyDown;
  get id(): string;
  private _onInput;
  private _onPaste;
  private _updateStatusText;
  private _handleValueChange;
  private _renderInput;
  componentWillLoad(): void;
  componentDidLoad(): void;
  render(): any;
}
