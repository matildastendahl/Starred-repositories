import { EventEmitter } from '../../stencil-public-runtime';
export declare class CTag {
  active: boolean;
  block: boolean;
  fit: boolean;
  flat: boolean;
  closeable: boolean;
  badge: string | number;
  size: 'default' | 'small';
  close: EventEmitter;
  private _onClose;
  render(): any;
}
