import { EventEmitter } from '../../stencil-public-runtime';
export declare class CAccordionItem {
  el: HTMLCAccordionItemElement;
  collapsable: boolean;
  heading: string;
  value: number | string;
  expanded: boolean;
  outlined: boolean;
  itemChange: EventEmitter;
  private _animation;
  private _accordionElement;
  private _accordionHeader;
  private _accordionContent;
  private _isCollapsing;
  private _isExpanding;
  private static _uniqueId;
  handleKeyDown(event: KeyboardEvent): void;
  stop(event: Event): void;
  private get _collapsedHeight();
  private get _expandedHeight();
  private _onToggleAccordion;
  private _collapse;
  private _expand;
  private _onAnimationFinish;
  componentWillLoad(): void;
  hasCustomHeader: boolean;
  hasIcon: boolean;
  componentDidLoad(): void;
  render(): any;
}
